# Environment variables for Gmail Job Tracker Application

# Flask Secret Key - Change this in production
SECRET_KEY=your-secret-key-change-this-in-production

# Google OAuth 2.0 Credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_PROJECT_ID=your-google-project-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# AWS Bedrock Credentials (for Job Board Clients - opted_job_links=TRUE)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1

# Bedrock Model ID
BEDROCK_MODEL_ID=us.amazon.nova-lite-v1:0

# OpenAI Configuration (for Regular Clients - opted_job_links=FALSE)
# Using Azure OpenAI Service
AZURE_OPENAI_ENDPOINT=https://engg-mhv8ardq-eastus2.cognitiveservices.azure.com
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_API_VERSION=2024-10-01-preview
AZURE_OPENAI_DEPLOYMENT=your-deployment-id-or-name
AZURE_USE_JSON_MODE=true



# Supabase Configuration
SUPABASE_URL=your-supabase-url
SUPABASE_KEY=your-supabase-key

# ==================================================
# Client Project Redirect Configuration (OPTIONAL)
# ==================================================
# These settings enable seamless integration with your client project.
# If not set, the app will show the built-in dashboard (backward compatible).

# CLIENT_REDIRECT_URL - Where to redirect after email processing completes
# Example: https://your-client-app.com/dashboard
# Example: http://localhost:3000/dashboard (for local development)
# CLIENT_REDIRECT_URL=

# CLIENT_LOGOUT_URL - Where to redirect after user logs out
# Example: https://your-client-app.com/login
# Example: http://localhost:3000/login (for local development)
# If not set, falls back to CLIENT_REDIRECT_URL or the built-in login page
# CLIENT_LOGOUT_URL=